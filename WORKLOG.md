# WORKLOG

Рабочий журнал проекта. Новые записи добавляются сверху.

## 2026-04-13 16:00 — Сравнение стратегий и адаптация версий
Изучено: документ `MoneyBot_Strategy_Comparison_and_Adaptation.md` с рабочего стола, а также текущее состояние `GridEngine`, executor-layer, аккаунтов, API и frontend-формы выбора биржи.
Сделано: применены выводы сравнения к документации и журналам; создана отдельная Obsidian-заметка `MoneyBot v2 - Сравнение стратегий и адаптация.md` с таблицей версий, картой соответствий legacy -> MoneyBot v2, метками изменений и разницей версий; обновлены Obsidian worklog и связанная документация, чтобы отразить мультибиржевую архитектуру и текущий adaptive-режим.
Выводы/риски: legacy-идеи adaptive/capitalization/reverse зафиксированы как карта соответствий, но не все они пока выделены как отдельные strategy types в коде; текущая реализация MoneyBot v2 покрывает базовый adaptive-контур и мультибиржевой executor-layer, а более сложные legacy-режимы стоит добавлять отдельно и осознанно.
Дальше: при необходимости превратить карту соответствий в отдельный технический backlog по strategy types, prepay-логике и расширенной adaptive-capitalization поддержке.

## 2026-04-13 15:16 — Интеграция Binance/Bybit в слой исполнителей
Изучено: документ `MoneyBot v2 - Codex Worklog` в Obsidian Vault, а также промпт и техническая документация по Binance/Bybit API с рабочего стола; сверена текущая реализация `ExchangeAccount`, `GridService`, `Executor` и фронтенд-страницы аккаунтов.
Сделано: добавлен общий `BaseExecutor` и новый пакет `app.strategy.executors` с фабрикой `create_executor` и `CcxtExecutor`; расширена поддержка `binance` и `bybit` в модели/схемах аккаунта; `ExchangeAccount` получил метод `to_executor`; `grid_service` и `/api/accounts/{id}/test` переведены на фабрику; на фронтенде добавлен выбор биржи в форме аккаунта; добавлены тесты на Bybit и фабрику исполнителей.
Выводы/риски: backend suite для затронутых сценариев проходит (`9 passed`), `ruff` зелёный, frontend production build успешен; текущая интеграция опирается на ccxt и paper/live совместимость сохраняется, но реальные ключи и testnet всё равно нужно проверять отдельно для каждой биржи.
Дальше: при необходимости добавить более богатый UI для теста подключения и расширить покрытие на реальные обменные ответы/ошибки без сетевых вызовов.

## 2026-04-09 18:22 — Оставлен рабочий Docker-стек для ручной проверки
Изучено: текущее состояние контейнеров после полного прогона проверок и доступность UI/API для ручного просмотра.
Сделано: подтверждён статус всех сервисов через `docker compose ps`; выполнен `docker compose up -d` для гарантии фонового запуска; проверена доступность backend (`/health` -> 200) и frontend (главная страница -> 200) внутри docker-сети.
Выводы/риски: проект оставлен в запущенном состоянии для ручного осмотра; backend опубликован на хост-порт `8001`, frontend на `5173`; для доступа в панель используется ранее созданный админ.
Дальше: пройти ручной UI-сценарий (логин, страницы grids/accounts/audit/profile), после проверки при необходимости остановить окружение командой `docker compose down`.

## 2026-04-09 18:20 — Docker-проверка и доведение quality gate до зелёного
Изучено: состояние docker-стека после `compose up`, поведение миграций на уже существующей схеме PostgreSQL, статусы backend/frontend/worker и результаты `ruff/mypy/pytest` в контейнере.
Сделано: поднят стек через Docker Compose; подтверждены ответы backend (`/health`, `/docs`) и доступность frontend; устранён рассинхрон миграций через `alembic stamp head` для существующей схемы; исправлены типовые проблемы (`mypy`) и линтерные замечания (`ruff`) в backend-коде и конфиге.
Выводы/риски: runtime и базовый API/UI smoke в Docker рабочие, backend quality gate зелёный (`ruff` и `mypy`), тесты проходят (`22 passed`); для прода остаются операционные задачи безопасности (смена временного админ-пароля, обязательный 2FA для админов).
Дальше: при необходимости провести расширенный ручной e2e-проход из UI (создание аккаунта, grid lifecycle, аудит) и зафиксировать финальный релизный чеклист.

## 2026-04-09 17:46 — Релиз-аудит и устранение блокеров
Изучено: фактическое состояние запуска и документации (`README`, `DEPLOYMENT`, `SECURITY`, `docker-compose`, `.env`), а также bootstrap БД в `backend/app/main.py` и `backend/worker/main.py`.
Сделано: убран auto-create схемы на старте backend/worker (чтобы схема создавалась только Alembic-миграциями); в worker добавлен безопасный bootstrap с предупреждением при неприменённых миграциях; добавлен отсутствующий `.env.example`; синхронизированы инструкции и URL/порты в `README`, `DEPLOYMENT` и `Makefile`.
Выводы/риски: устранены ключевые эксплуатационные расхождения (отсутствие `.env.example`, несогласованные порты в инструкциях, двойной bootstrap схемы); остаётся подтвердить финальный статус через `lint/test` и чистый миграционный прогон.
Дальше: прогнать `make lint`, `make test`, проверить сценарий `up -> migrate` на чистом volume и обновить итоговый go/no-go.

## 2026-04-08 16:31 — Создание первого superadmin
Изучено: CLI bootstrap-поток `backend/cli/create_superadmin.py`, ограничения валидации email и политики паролей для первого входа в панель.
Сделано: в работающем Docker-окружении создан первый пользователь `superadmin` через `docker compose exec backend python -m cli.create_superadmin`; рабочая учётная запись создана с email `admin@mail.ru`.
Выводы/риски: комбинация `admin/admin` не проходит текущие ограничения проекта, потому что пароль должен быть не короче 8 символов и содержать буквы и цифры, а email проходит через строгую валидацию; для входа выдан временный пароль, который лучше сменить после первого логина.
Дальше: при необходимости включить 2FA для административной учётной записи и создать остальных пользователей через invite flow из панели.

## 2026-04-08 16:30 — Запуск продукта в Docker
Изучено: текущий `docker-compose` сценарий, переменные окружения, статус Docker Desktop, проброс портов и запуск backend/worker/frontend в локальном окружении.
Сделано: создан локальный `.env` для dev-запуска; подняты контейнеры `postgres`, `redis`, `backend`, `worker`, `frontend`; устранён конфликт хостового порта `8000` через перенос backend на `8001`; подтверждён startup backend и ответ `/health` изнутри контейнера; исправлена миграция `backend/migrations/versions/20260408_0001_initial_schema.py`, чтобы PostgreSQL enum-типы не создавались повторно.
Выводы/риски: продукт запущен локально через Docker Compose, UI доступен на `5173`, API прокинут на `8001`; внешний `curl` к host-порту в этой среде не отработал, поэтому доступность backend подтверждена через `docker compose exec`; миграции всё ещё конфликтуют с уже авто-созданной схемой из `init_models()`, если накатывать их поверх уже поднятой dev-базы.
Дальше: отдельным проходом убрать дублирование между auto-create схемы и Alembic-миграциями, затем проверить чистый bootstrap базы через `make migrate` на пустом volume.

## 2026-04-08 15:35 — Code review и исправление багов
Изучено: runtime-цепочка `API -> grid_service -> worker`, auth/2FA flow, права доступа к сеткам, bootstrap авторизации на фронтенде и полный backend test suite.
Сделано: исправлены критичные баги в `backend/app/services/grid_service.py`, `backend/app/api/grids.py`, `backend/app/api/auth.py`, `backend/app/strategy/paper_executor.py`, `backend/worker/main.py`, а также bootstrap-auth на фронтенде в `frontend/src/App.tsx`; добавлены регрессионные тесты на 2FA и ownership сеток.
Выводы/риски: убран риск дублирования ордеров между backend и worker, закрыт доступ к чужим сеткам по UUID, исправлен broken/insecure 2FA setup/verify flow; после правок backend test suite проходит полностью (`22 passed`), frontend build остаётся зелёным.
Дальше: отдельным проходом можно добить `ruff` и более строго пройтись по качеству типов и code style, но основные поведенческие баги по критичным зонам уже закрыты.

## 2026-04-08 15:22 — Реализация шагов 3-6
Изучено: `CODEX_TASK.md`, текущий скелет backend/frontend, ограничения тестового окружения на локальной машине, поведение импортов `structlog`, `bcrypt` и `ccxt` в Python 3.14.
Сделано: реализованы `GridEngine`, `PaperExecutor`, `LiveExecutor`, типы стратегии и unit-тесты; добавлены backend-модули auth/security/2FA/crypto/audit, REST и WebSocket API, сервис сеток, worker, CLI суперадмина, первая миграция; frontend-заглушка заменена на панель с логином, дашбордом, сетками, аккаунтами, пользователями, аудитом и профилем; обновлены `README.md`, `docs/DEPLOYMENT.md`, `docs/SECURITY.md`.
Выводы/риски: backend-тесты проходят (`20 passed`), frontend production build проходит; `ruff` сейчас даёт много стилевых замечаний по новым файлам, а `mypy` не удалось дождаться до финального статуса в разумное время, поэтому функциональная часть подтверждена лучше, чем чистота линтера.
Дальше: при следующем проходе добить `ruff/mypy`, сузить frontend bundle, затем провести ручной прогон через Docker Compose и проверить сценарии `paper`/`worker` уже в контейнерах.

## 2026-04-08 14:27 — Локальный журнал работ
Изучено: текущая структура репозитория и потребность вести единый журнал прямо в проекте без отдельной интеграции с Obsidian.
Сделано: создан файл `WORKLOG.md` в корне проекта; зафиксирован единый формат записей с полями "Изучено", "Сделано", "Выводы/риски" и "Дальше".
Выводы/риски: один Markdown-файл в репозитории достаточно прост для постоянного ведения и не зависит от внешних инструментов; новые записи будут добавляться сверху, чтобы последнее состояние было видно сразу.
Дальше: пополнять журнал после каждого существенного этапа работы по проекту.

## Шаблон записи
```md
## YYYY-MM-DD HH:MM — Короткое название
Изучено: ...
Сделано: ...
Выводы/риски: ...
Дальше: ... / —
```

## 2026-04-08 14:03 — Изучение архитектурного документа
Изучено: документ `01_ARCHITECTURE.md` со стеком, схемой сервисов, моделью данных, логикой адаптивной сетки, требованиями по безопасности и дорожной картой.
Сделано: подготовлено краткое резюме архитектуры; выделены ключевые решения: `FastAPI + PostgreSQL + Redis + React`, отдельный `worker`, единая стратегия для `paper` и `live` через абстракцию `Executor`, роли, аудит и шифрование API-ключей.
Выводы/риски: архитектура выглядит целостной и годится как основа для реализации; критично сохранить единый код стратегии для двух режимов и аккуратно разделить конфигурацию сетки, ордера и события в БД.
Дальше: использовать документ как основу для следующих этапов реализации и организационных решений по проекту.
